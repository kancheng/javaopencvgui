使用 Java 完成 GUI ，類似相簿的兼 OpenCV 處理圖像的預覽功能。

1. 可以給路徑找到指定的目錄。

2. 可以逐張檢視圖像。

3. 分析目錄中的所有圖片，每張圖片的詳細資訊大小、格式、通道數。

4. 統計整個目錄圖片的詳細資訊大小、格式、通道數。

5. 跟 OpenCV 結合，將 OpenCV 的參數可以 GUI 操作，運用 OpenCV 的參數，來模擬被  OpenCV 改變後的狀態。確定後輸出一個 JSON 紀錄的 OPENCV 的參數格式檔案。
